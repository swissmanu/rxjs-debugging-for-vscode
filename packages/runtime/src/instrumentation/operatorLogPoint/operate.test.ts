describe('runtime', () => {
  describe('operate()', () => {
    test.todo('');
    // const operator: jest.Mock<ReturnType<MonoTypeOperatorFunction<number>>, Parameters<MonoTypeOperatorFunction<number>>> = jest.fn((source) => source);
    // const source = new Subject<number>();
    // const operated = operate()
  });
});
